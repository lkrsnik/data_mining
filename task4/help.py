def cost(theta, X, y):


def grad(theta X, y):
    Theta = theta.reshape((n_class, X.shape[1]))
    grad = None
    return grad.ravel()


import numpy as np
A=np.rand(3,4)
A.shape
np.allclose(A, A.ravel().reshape((3,4)))